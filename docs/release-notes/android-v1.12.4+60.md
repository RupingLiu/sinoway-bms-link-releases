# Sinoway BMS Mate 1.12.4+60

## 简体中文

- 修复鸿蒙卓易通环境安装后首次启动时底部导航栏显示黑色的问题：应用会在首帧绘制后和每次回到前台时重新声明系统栏样式。
- 应用更新提示改版：更新包下载完成后弹出一次含版本说明的对话框（可稍后或立即安装），并将常驻提示改为底部深色浮动条，不再遮挡页面顶部。
- 补齐 Android 自适应桌面图标，修复鸿蒙桌面图标不满幅、边沿残留底色的问题。

## 繁體中文

- 修復鴻蒙卓易通環境安裝後首次啟動時底部導覽列顯示黑色的問題：應用會在首幀繪製後和每次回到前景時重新宣告系統列樣式。
- 應用更新提示改版：更新包下載完成後彈出一次含版本說明的對話框（可稍後或立即安裝），並將常駐提示改為底部深色浮動條，不再遮擋頁面頂部。
- 補齊 Android 自適應桌面圖示，修復鴻蒙桌面圖示不滿幅、邊沿殘留底色的問題。

## English

- Fixed the black system navigation bar on first cold start after installation under HarmonyOS compatibility environments: the app now reasserts its system bar style after the first frame and on every resume.
- Revamped the update prompt: when an update package finishes downloading, a one-time dialog with release notes appears (install now or later), and the persistent prompt is now a dark floating bar at the bottom instead of covering the top of the page.
- Added Android adaptive launcher icons, fixing the icon not filling the mask and leaving edge residue on HarmonyOS home screens.

## Assets

- Gitee APK: `Sinoway BMS Mate 1.12.4 60.apk`
- GitHub APK: `Sinoway.BMS.Mate.1.12.4+60.apk`
- Size: `21084157` bytes
- SHA-256: `7d6adceec56d016c1f1a875d599c1e0359c0ef05c7ff9c6b787384c8f8543194`

The manifest uses the verified Gitee release attachment as the primary APK download source and keeps the GitHub Release asset as the backup mirror.
