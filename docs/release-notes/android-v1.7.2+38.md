# Sinoway BMS Link Android 1.7.2+38

## 简体中文

优化蓝牙扫描页布局：扫描到多个设备时，设备列表会按屏幕空间动态展开并支持内部滚动，减少空白区域；未连接提示保留为轻量提示行，不再挤占设备列表。

## 繁體中文

優化藍牙掃描頁佈局：掃描到多個設備時，設備列表會依螢幕空間動態展開並支援內部捲動，減少空白區域；未連接提示保留為輕量提示列，不再擠佔設備列表。

## English

Improved the Bluetooth scan layout: when multiple devices are discovered, the device list expands with the available screen space and scrolls internally, reducing empty space. The disconnected hint remains as a lightweight row instead of taking room away from scan results.

## Assets

- APK: `sinoway-bms-link-android-v1.7.2+38.apk`
- Size: `20074375` bytes
- SHA-256: `f0c97de53696d95c65ab929f72c36b30de81b581189eddf2eac8065cf9a319cb`

The current manifest uses the Gitee release attachment as the primary APK download source. GitHub Release assets remain in `downloadMirrors` as the backup endpoint.