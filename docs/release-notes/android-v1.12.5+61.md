# Sinoway BMS Mate 1.12.5+61

## 简体中文

- 修复从总览页「设备总数」卡片进入设备列表时，状态筛选未重置为「全部」、仍停留在上次所选（如「在线」）的问题。

## 繁體中文

- 修復從總覽頁「裝置總數」卡片進入裝置清單時，狀態篩選未重設為「全部」、仍停留在上次所選（如「在線」）的問題。

## English

- Fixed the status filter not resetting to "All" when opening the device list from the Overview "Total Devices" card; the list previously kept the last-selected filter such as "Online".

## Assets

- Gitee APK: `Sinoway BMS Mate 1.12.5 61.apk`
- GitHub APK: `Sinoway.BMS.Mate.1.12.5+61.apk`
- Size: `21084157` bytes
- SHA-256: `716c93e892fdc504e349c13f824b43e96a00256b0600b386ce90eba175eee04e`

The manifest uses the verified Gitee release attachment as the primary APK download source and keeps the GitHub Release asset as the backup mirror.
